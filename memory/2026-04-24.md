# 2026-04-24 工作日志

## interview-master APK 打包全流程修复

### 背景
主人需要通过 GitHub Actions 自动打包 interview-master APK。
项目是 React Native 0.74 + Expo SDK 55（但实际 Expo 未使用）

### 发现的问题（按优先级）
1. `package.json` 有 Expo 依赖但代码不使用 → 移除
2. `android/` 目录配置被破坏（Expo SDK 55 配置混入 bare RN 项目）→ 还原
3. `ic_launcher_background.png` 和 `.xml` 重复 → 删 PNG
4. `com.anonymous/interviewmaster/` 错误包目录 → 删除
5. `build.gradle`/`app/build.gradle`/`gradle.properties` 配置被改坏 → 还原
6. workflow 有并行构建问题 → 合并步骤

### 修复后的状态
- ✅ 构建 #103 成功，APK 大小 37.5 MB
- 仓库：https://github.com/Kkwans/interview-master （已废弃，主人计划归档/删除）
- Workflow：push main 分支自动触发构建
- APK 下载：GitHub Actions Run 页面 artifacts

### GitHub Token
主人提供了 `[REDACTED]`
用于 GitHub API 操作和 `gh` CLI 认证

### 重要教训
1. 04-18 成功的构建配置（fce56d1c）才是正确的基准
2. 所有之后的 Expo 相关修改都是破坏性的
3. 本地验证和远程不同时，要以 GitHub 为准
4. 多次修复没效果时要回退到已知成功的版本

## 新任务：影视爬虫 (movie-crawler-0424)

### 目标站点
- https://www.pkmp4.xyz/（七味网）
- 基于 MacCMS 系统

### 站点结构
- 首页 `/` — 最新推荐影视
- 电影详情 `/mv/{id}.html` — 含磁力+网盘下载资源
- 电视剧列表 `/vt/{page}.html`
- 综艺列表 `/vs/{page}.html`
- 动漫列表 `/s/{page}.html`

### 关键发现
- 资源类型：magnet、百度网盘、夸克网盘、迅雷、UC网盘、ed2k
- 海报 CDN：https://i1.vvmp4.com/
- AJAX 接口：`/index.php/ajax/suggest?mid=1&wd=关键词`
- 静态 HTML 页面，无需 JS 渲染即可爬取

### 当前阶段
主人正在审核 EXPLORE.md，待确认需求后再执行爬取

### 主人 GitHub Token
[REDACTED]（已保存于今日记忆）
