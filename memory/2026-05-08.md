# 2026-05-08 日记忆

## UGOS Docker 面板注册机制调研

### 核心发现
- 面板通过 SQLite 数据库管理 compose 项目
- **数据库路径**: `/volume1/@appstore/com.ugreen.docker/db/docker_info_log.db`
- **表名**: `compose`
- CLI 创建项目需手动注册到数据库才能在面板显示
- 已更新 DOCKER_RULES.md 4.6 节

## 影视森林全面优化

### UX修复7项（已部署）
1. 移动端底部Tab即时反馈（useTransition）
2. 移动端点击悬停动画移除（tap-highlight-color）
3. 影视卡片统一大小（minHeight:60px）
4. 点击卡片进入详情即时响应（prefetch + navigating状态）
5. 卡片底部排版对齐（flex justify-between）
6. 升序降序按钮SVG重设计
7. 搜索结果页排序功能

### 用户系统（已部署）

**后端**:
- 3张新表：user / user_movie_list / user_movie_list_item
- 15个新文件：实体/Mapper/Service/Controller/JWT/过滤器/配置
- BCrypt密码加密 + jjwt 0.12.6
- 注册自动创建3个默认片单（想看/在看/看过）
- 默认片单不可删除

**前端**:
- 登录/注册页面
- "我的"页面 + 片单详情页
- 收藏弹窗组件（CollectModal）
- MobileBottomNav新增"我的"Tab
- Header用户菜单（PC端右上角）
- MovieCard + 所有详情页收藏按钮

### Git推送
- sandbox环境git push超时，改用GitHub REST API推送成功
- 4个仓库全部推送完成

### 教训
- 不要凭猜测下结论，先调研再回答
- sandbox网络有限制，git push需用API替代
- 规范流程（提交/推送/文档）应自动执行，不应等催
- 部署前必须自测，不能把未验证的代码推给主人

## 用户系统bug修复（9项）

### 修复内容
1. 默认片单可点击进入列表页
2. 片单详情页改为横向列表布局（参考豆瓣）
3. 片单数量正确显示
4. Header移动端隐藏登录按钮，PC端只显示登录
5. 收藏按钮单击加入想看，双击弹出片单选择
6. 升序降序按钮修复
7. 控制台报错修复
8. 默认头像改为首字母圆形头像
9. 移动端卡片标题允许2行，收藏按钮缩小

### 部署
- 前端构建部署到NAS ✅
- GitHub推送 ✅
- 服务验证：用户端(3000)✅ 登录页(200)✅ 个人页(200)✅

## 全面优化第4轮（10项）

### 后端修改
- getUserLists 返回 item_count
- addItem 支持 rating 和 note 参数
- getListItems 返回完整信息
- 排序支持 updateTime/豆瓣/IMDB/烂番茄

### 前端修改
- 搜索结果页PC端2列布局，显示完整信息
- 片单列表页横向布局，显示评分/备注/收藏时间
- 排序支持最新更新/上映时间/豆瓣/IMDB/烂番茄
- 登录错误处理优化

### 部署
- 后端Maven构建+部署 ✅
- 前端Next.js构建+部署 ✅
- 服务验证 ✅
- GitHub推送 ✅
