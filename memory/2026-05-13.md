# 2026-05-13 日记忆

## 管理端全面优化 v2（00:30 - 01:00）

### 任务概述
主人提出 12 个大类问题，要求全面优化管理端前后端

### 完成的工作

**后端 (admin-server)**:
1. CrawlerCore 新增 6 个字段提取方法：
   - extractLanguage(): 从语言链接提取
   - extractDuration(): 从"片长：xxx分钟"提取
   - extractReleaseDate(): 从"上映："文本提取
   - extractAlias(): 从"又名："文本提取
   - extractWriter(): 从"编剧："标签提取
   - extractImdbScore(): 从 IMDB 链接提取
2. 5 种内容类型（Movie/Drama/Variety/Anime/ShortDrama）统一使用新字段
3. ShortDrama 修复：移除不存在的 writer 字段

**前端 (admin-ui)**:
1. api.ts: 修复重复拦截器
2. select.tsx: 自定义 Select 组件（搜索、多选）
3. toast/dialog/modal: 样式优化
4. 仪表盘: 重新设计布局
5. 内容管理: 新增 language/writer/duration/releaseDate/alias/scoreImdb 字段，操作后自动刷新
6. 数据统计: recharts 饼图+柱状图，修复百分比计算
7. 登录页: 移至 (auth) 路由组

### 部署
- 后端 JAR: NAS 8081 ✅
- 前端: NAS 3001 ✅
- GitHub: admin-ui + admin-server ✅
- 阿里云 ACR: admin-server 镜像 ✅

### 教训
- 子 agent 超时问题严重（2 次都超时），大任务需要自己做
- NAS Docker Maven 构建使用 maven-repo named volume 缓存依赖
- ShortDrama 没有 writer 字段，爬虫代码需要注意实体字段差异

## 飞书复读机 bug 修复（20:44 - 20:55）

### 问题
- 前一个会话中无论主人发什么，我都复读同一句话
- 主人转发了聊天记录证实

### 根因
- session 配置为空，会话永不重置
- 上下文无限膨胀（子 agent + 工具调用 + cron 汇报）
- mimo 模型在超长上下文下退化成复读机

### 修复
- openclaw.json 添加 session.reset 配置：
  - direct 会话空闲 6 小时自动重置
  - 每天凌晨 4 点全局重置
- 网关已热重载（SIGUSR1）
- MEMORY.md 已更新教训
