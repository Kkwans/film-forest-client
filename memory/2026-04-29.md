## 前端页面开发（04-29 早）

### 完成
- **电影列表页** `/movie`：地区/年代筛选，骨架屏加载，电影卡片网格
- **电影详情页** `/movie/[id]`：封面/元数据展示，在线播放Tab + 磁力下载Tab
- **搜索页** `/search`：关键词搜索，Suspense 边界修复，fallback mock 数据
- **构建通过**：`next build` 成功，6 个路由全部生成

### 技术修复
- 类型错误：`Movie` 从 `@/stores/movieStore` 而非 `@/lib/api` 导入
- axios response 类型：`as any` 解决 `.data` 属性推断问题
- Select onValueChange：`v => setX(v || 'all')` 处理 null
- useSearchParams：包装 Suspense 边界

### GitHub push 失败
- 网络问题，超时。需检查 NAS 网络到 GitHub 的连通性
- 代码已在本地提交，待网络恢复后推送

### 下一步
1. 管理端爬虫管理界面 `/crawler`（核心功能）
2. Docker 部署前后端
3. 爬虫模块开发

## 主题切换功能（04-29 早 加）

### 完成
- **默认浅色主题**：CSS 变量 `--bg-primary` / `--text-primary` 等
- **Header 深色切换按钮**：☀️/🌙 手动切换，localStorage 持久化
- **系统自适应**：`prefers-color-scheme` 检测，已打开页面需刷新
- **全局样式覆盖**：layout / Header / Footer / page.tsx 全部使用 CSS 变量
- **优先级低**：其他基础功能完成后再做深色模式完善

### CSS 变量定义（globals.css）
```css
:root { --bg-primary: #f5f5f4; --bg-secondary: #ffffff; --accent: #059669; ... }
.dark { --bg-primary: #09090b; --bg-secondary: #18181b; --accent: #10b981; ... }
```

### 注意事项
- 深色模式优先级低，先完成其他基础功能
- 手机/电脑系统深色模式：页面打开后刷新才生效（非重点）